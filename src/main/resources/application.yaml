server:
  port: ${PORT:8080}
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/KazRent1
    username: postgres
    password: erke
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect